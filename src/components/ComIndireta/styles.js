import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    painel: {
        backgroundColor: '#333',
        padding: 20,
        marginVertical: 15,
        borderRadius: 25,
        
    }
});

export default styles;