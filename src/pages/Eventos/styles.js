import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    painel: {
        backgroundColor: '#333',
        marginVertical:20,
        minHeight: 80,
        padding: 20,
        borderRadius: 20,
    }
});

export default styles;